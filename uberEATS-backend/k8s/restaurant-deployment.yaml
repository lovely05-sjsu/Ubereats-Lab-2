apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: restaurant-service
  template:
    metadata:
      labels:
        app: restaurant-service
    spec:
      containers:
        - name: restaurant-service
          image: mansi061202/ubereats-backend-restaurant-service:latest
          ports:
            - containerPort: 2002
          env:
            - name: PORT
              value: "2002"
            - name: DB_HOST
              value: mysql
            - name: DB_PORT
              value: "3306"
            - name: DB_USER
              value: root
            - name: DB_PASSWORD
              value: mysql
            - name: DB_NAME
              value: ubereats_db
            - name: DB_DIALECT
              value: mysql
            - name: KAFKA_BROKER
              value: kafka:9092
